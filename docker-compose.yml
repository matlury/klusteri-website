version: '3.1'

services:
  api:
    build:
      context: "./backend"
      dockerfile: Dockerfile
    ports:
      - "8000:8000"

  frontend:
    build:
      context: "./frontend"
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - api
